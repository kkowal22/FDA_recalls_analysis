# Systemic Reform or Targeted Priority? Evaluating Latency and Queue-Depth Behavior Across FDA Recall Classifications

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kkowal22/FDA_recalls_analysis/blob/main/Class_2_3_Latency_Replication/class23_latency.ipynb)

## Overview
This directory contains a comparative replication analysis examining whether the decade-long administrative latency decline (2013–2019) and tail-risk improvement observed in **Class I FDA drug recalls** replicate across lower-severity **Class II and Class III recalls**. 

While Class I recalls represent immediate, life-threatening risks, Class II and Class III recalls comprise the vast majority of total recall volume. By scaling up the dataset to include these lower-priority tiers, this analysis tests whether FDA operational enhancements reflected agency-wide systemic reforms or targeted prioritization strictly limited to high-severity events.

---

## Core Research Questions

1. **Latency Trend Replication:** Does Class II/III administrative latency (Recall Initiation → Center Classification) follow the same sharp 2013→2019 decline-then-plateau shape, or is the plateau unique to Class I recalls?
2. **Workload Independence & Statistical Power:** Does the volume-latency independence observed in Class I ($r=0.127, p=0.745$) hold for Class II/III, or does workload start to impact processing times when statistical power increases across a significantly larger sample size ($N$)?
3. **Tail-Risk Backlog Reduction:** Does the $p_{90}$ tail-risk improvement ($299 \rightarrow 61$ days) show a comparable magnitude in Class II/III, or is eliminating worst-case backlogs uniquely a Class I achievement?
4. **Queue-Depth Dynamics:** Does the open-caseload time series show a persistently higher queue depth for Class II/III recalls, corroborating or contradicting the displacement hypothesis?

---

## Pipeline Architecture *(In Progress)*

| Step # | Notebook Code Block | Action Executed in Code | Technical / Analytical Rationale |
| :---: | :--- | :--- | :--- |
| **1** | Environment Setup & Storage Mount | Mounted Google Drive to Colab (`/content/drive`) to expose cloud directory paths. | Establishes physical path access to raw data storage before initiating file reading operations. |
| **2** | Libraries Installation & Imports | Installed `pandera` and imported standard data engineering libraries (`glob`, `os`, `numpy`, `pandas`, `pandera`). | Loads execution dependencies for path matching, tabular manipulation, and schema validation into the active Python kernel. |
| **—** | **Data Processing** | **—** | **—** |
| **3** | Lean Six Sigma Palette Configuration | Defined `SIXSIGMA_PALETTE` dictionary establishing standardized hex codes for process visuals. | Configures global visual encoding standards early to ensure consistent chart styling throughout downstream analyses. |
| **4** | File Path Matching & CSV Ingestion | Set directory path, retrieved matching CSV files via `glob.glob()`, and read files into `df_list` while appending `source_file` metadata as the trailing column. | Locates all 22 batch files and loads them into memory, preserving record-level lineage back to individual raw source files. |
| **5** | Pre-Merge Schema Validation Audit | Normalized column names via `.str.strip().str.lower()`, extracted schema sets across all loaded DataFrames, and printed divergent/missing column reports. | Diagnoses column structural discrepancies (e.g., extra lot number fields in older files) across files prior to executing concatenation. |
| **6** | Code Info Field Reconstruction | For Jan 2016 to Oct 2016.csv only: concatenate Code Info + More Code Info + More Code Info.1 + More Code Info.2 back into a single Code Info string (2 affected rows: Event ID 74057, Event ID 74745), then drop the 3 overflow columns from the merged dataframe. | Restores full lot-level detail lost to a field-length overflow during export, without altering row count or event structure. Moved ahead of concatenation so every file has a uniform column set before merging — the overflow was a per-file artifact discovered in Step 5 and must be resolved before Step 7's concat, not after. |
| **7** | Concatenation & Null Standardization Audit | Merged `df_list` into `combined_df` via `pd.concat(ignore_index=True)`, converted empty spaces/placeholders to true `NaN`, and executed initial missing value audit. | Unifies 22 files into a single master structure (`df_clean`) and audits non-standard missing value flags across the unified record set. Now runs on a schema-uniform `df_list` following Step 6's reconstruction. |
| **8** | Placeholder Null Discovery & Standardization | Programmatically scan every column for case-insensitive, whitespace-stripped matches against a placeholder token list (unknown, none, n/a, null, etc., discovered via full-value audit rather than assumed) BEFORE converting; flag matches in new boolean column `was_reported_unknown`; then convert all matched placeholder values to true `NaN` in a single uniform pass across all columns. | Distinguishes FDA-reported "unknown" from genuinely blank fields without forking the downstream imputation logic, and ensures the placeholder list is built from an actual audit of raw values rather than assumed from memory. Runs immediately after Step 7's initial audit and before Step 9's imputation, since imputation must act on a fully standardized null set. |
| **9** | Multi-Type Data Imputation & Cleanup | Applied categorical placeholders ('Not Recorded', 'Unknown'), converted date fields, filled numeric missing values with medians, and verified remaining nulls. | Systematically addresses missing data across text, date, and numeric field types to finalize a fully cleaned dataset ready for latency analysis. Moved after Step 8 so imputation fills a dataset where true nulls and FDA-reported "unknown" values have already been distinguished and standardized. |
| **10** | Pre-Dedup Date Consistency Validation | Group by Event ID, check `nunique()` on Recall Initiation Date and Center Classification Date; assert/flag any Event ID with more than one distinct value in either field. | Confirms the safety of collapsing to one row per Event ID for latency calculations before doing so — guards against silently computing latency from the wrong row if a future data pull violates the event-level date consistency observed in this dataset. Runs on the fully cleaned, date-parsed dataset produced by Step 9. |
| **11** | Event Population Definition & Cross-Project Overlap Disclosure | Define Class II/III event population as `df.drop_duplicates(subset=['Event ID'], keep='first')` applied to the classification-filtered slice (rows where Classification is Class II or Class III) — mirroring the exact dedup logic used in the Class I project, with no attempt to assign a single "resolved" classification per event. Document the 69 Event IDs known to also appear in the Class I population (from Step 10's validation) as an explicit, expected overlap rather than a defect. | Keeps dedup logic identical across both GitHub projects for methodological consistency; population membership reflects presence in a classification-filtered slice (as FDA's own export already did for Class I), not manufactured exclusivity — since no FDA documentation defines a canonical event-level classification for events spanning multiple severities, avoiding that invented rule is more defensible than fabricating one. The 69-event overlap is carried forward into latency calculations and disclosed in the methodology notes rather than removed. This final step depends on Step 10's validation having passed. |
| **—** | **Data Analysis** | **—** | **—** |
| **12** | Start-Date Cutoff, Core Latency Field & Year Assignment | Filtered `class123_events` to recalls initiated on or after 2013-01-01 (same cutoff as the Class I project), then computed `Latency_Days`, assigned `Year`, computed `Report_Lag`, and flagged `Reliable` events using a 90th-percentile reporting-lag safety margin. | Applies the identical start-date boundary used in the Class I project for direct methodological consistency between the two studies. `Reliable` guards downstream latency metrics against right-censoring bias from recent, still-open events. |
| **13** | Backlog-Signal Metrics per Year & Classification | Grouped by `['Year', 'classification']` to compute `total_recall_volume` on the full population and `median_latency`, `p90_latency`, `pct_over_60`, `pct_over_90` on the `Reliable` subset. Merged into `annual_metrics`. | `total_recall_volume` is censoring-immune (recall initiations are known immediately); the four latency metrics are not, so they're restricted to `Reliable` events. Grouping by classification (a deliberate departure from the Class I-only script) preserves the ability to compare Class I vs. II/III latency shape — the notebook's core research question — rather than collapsing all three severities into one blended trend. |
| **14** | Volume–Backlog Relationship Test (event-level) | Computed `Concurrent_Volume` per event via an O(n log n) `searchsorted` count of recalls (any classification) initiated within a ±45-day window. Ran a raw correlation and an OLS regression of `Latency_Days` on `Concurrent_Volume` controlling for `Year` and `classification`; cross-checked via group-mean detrending. | Tests whether processing backlog is associated with individual-event latency, independent of year-level trend and baseline class-level latency differences. `Concurrent_Volume` is intentionally system-wide (not same-class) to capture FDA-wide queue pressure; C(classification) was added to the regression since the Class I-only script had no class variable to control for and omitting it would let between-class latency differences masquerade as a volume effect. |
| **15** | Queue-Depth (Open-Caseload) Time Series | For each month-end snapshot date T across the data range, counted events open at that moment (Recall Initiation Date ≤ T and Center Classification Date > T), computed separately per classification rather than pooled; assembled into `queue_df` (Snapshot, classification, `Open_Events`). | Produces a month-by-month backlog time series broken out by severity class, needed as the input for the queue-depth visualization. Grouping by classification (a departure from the single-class Class I script) is required so Class I, II, and III backlog trajectories can be plotted and compared on the same time axis rather than blended into one line. Runs on `class123_events` after Step 12; closes out the Data Analysis section. |
| **—** | **Data Visualization** | **—** | **—** |
| **16** | Median vs P90 Latency by Year, by Classification | Melted `annual_metrics` (`id_vars` now Year + classification) into long form; built a line chart of `median_latency` and `p90_latency` by Year, row-faceted by classification with a shared y-axis. | Row-faceting (a departure from the Class I script's single panel) keeps the Median-vs-P90 color coding legible per class while stacking all three classes for direct visual comparison — a single panel would overlay 6 lines and obscure the shape comparison this notebook exists to make. |
| **17** | Percentage of Recalls Exceeding Threshold, by Year and Classification | Melted `annual_metrics` into long form (`pct_over_60`, `pct_over_90`); built a grouped bar chart by Year, row-faceted by classification with a shared y-axis. | Same faceting rationale as Step 16, applied to the threshold-breach metrics — keeps the >60-day/>90-day bar comparison readable across three classes instead of six overlapping bar pairs on one panel. |
| **18** | FDA Recall Monthly Queue Depth by Classification (2013–2026) | Built a multi-line time series from `queue_df`, colored by classification, in place of the Class I script's single gradient-filled area. | A single-series gradient fill only reads cleanly for one line; with three classes now in `queue_df`, three overlapping filled areas would occlude each other and misrepresent overlap as intensity. A colored multi-line chart is the direct, honest way to compare three backlog trajectories on a shared axis. |
| **19** | Year-Level Volume by Classification | Built a grouped bar chart of `total_recall_volume` by Year from `annual_metrics`, colored and offset by classification; combined with the Step 16 latency chart via vertical concatenation. | The Class I script's volume bar was a single uncolored series (color carried no information with one class). With three classes, color/offset by classification is required just to keep each year's three bars distinguishable rather than silently overwriting or stacking. |
| **20** | Concurrent Volume vs Detrended Latency, by Classification | Built a scatter of `Detrended_Volume` vs `Detrended_Latency` from `reg_df`, colored by classification, with regression trend lines fit separately per classification (`transform_regression` grouped by classification) rather than one pooled line. | Regression coefficient already answers whether a pooled volume-latency relationship exists; per-class trend lines test the sharper question of whether that relationship differs by class (e.g., backlog predicting latency for Class II/III but not Class I) — a pooled line would visually erase that distinction. Detrending already removed both Year and classification baseline effects (Step 14), so color-coding by class here shows genuine within-class deviation, not a confound. |
| **—** | **Image Exporting** | **—** | **—** |
| **21** | Export All Charts to Google Drive | Scanned `globals()` for any Altair chart object and saved each as a PNG to `/content/drive/MyDrive/FDA_Recall_Analysis/charts_class123`. | Automates chart export without manually naming/tracking each figure. Output directory renamed from the Class I script's charts to `charts_class123` so this doesn't silently overwrite the Class I project's saved PNGs in the same shared Drive folder. |
---

## Repository Structure

```text
Class_2_3_Latency_Replication/
├── README.md                      # Folder documentation
├── class23_latency.ipynb          # Main execution notebook (ingestion, audit, latency metrics)
└── Raw CSV Files/                  # 22 date-chunked raw CSV files (June 2012 – July 2026)
